# Deny PHP execution in uploads directory
<FilesMatch "\.php$">
    Deny from all
</FilesMatch>

# Only allow images
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Allow from all
</FilesMatch>

Options -Indexes
